cd <directory>

Changes the current directory to the given directory. If no directory is given, changes the current
directory to the home directory.

Examples:

Go back to the home directory:
	
	cd

Go to the pictures directory:

	cd pictures/

Go to the parent directory

	cd ..

